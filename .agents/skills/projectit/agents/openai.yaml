interface:
  display_name: "Projectit"
  short_description: "Repo-specific patterns and workflows for projectit"
  default_prompt: "Use the projectit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true